--- src/modules/owner-analytics/owner-analytics.service.ts
+++ src/modules/owner-analytics/owner-analytics.service.ts
@@ -167,8 +167,11 @@
         cancellationRate:
           totalBookings > 0
             ? ((cancelled.length / totalBookings) * 100).toFixed(1) + '%'
-              : '0',
-        },
+            : '0%',
+        noShowRate:
+          totalBookings > 0
+            ? ((noShows.length / totalBookings) * 100).toFixed(1) + '%'
+            : '0%',
       };
     }
